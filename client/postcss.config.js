// PostCSS configuration intentionally left minimal.
// Using the @tailwindcss/vite plugin for Tailwind processing; keep this file present
// to avoid tooling that expects a PostCSS config, but do not register any plugins here.
export default {
  plugins: {},
}
